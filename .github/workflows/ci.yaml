name: CI

on: push

jobs:
  yaml-lint:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout project
        uses: actions/checkout@v3
      - name: lint yaml
        uses: ibiqlik/action-yamllint@v3
